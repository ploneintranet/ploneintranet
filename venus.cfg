[buildout]
extends =
    buildout.d/base.cfg
    buildout.d/solr.cfg
    buildout.d/zeo.cfg

package-extras = [solr,test]
develop =
versions = versions

parts +=
      supervisor

[scripts]
eggs = ${buildout:package-name} ${buildout:package-extras}

[versions]
ploneintranet = 1.0.0a2